# DITING — 整站信息架构重设计 Brief

> 这份文档用来喂给 claude.ai/design 的设计 agent。把整份贴进去，让它基于现状重新设计整站的信息架构与交互流程。

---

## 1. 产品是什么

DITING 是一个**社区协作的游戏汉化（翻译）平台**。玩家可以浏览某款游戏的官方"主集"（原文 + 当前译文），Fork 出自己的副本翻译，然后提交"合并请求"把改动并回主集。管理员审核并合并。类似 "GitHub 的 Pull Request 流程，但用于游戏文本翻译"。

目前支持多款游戏（如 Persona 2: Innocent Sin / Eternal Punishment），按 `platform/gameSlug` 隔离，每款游戏有独立的主集、合并请求、问题反馈、术语表。

---

## 2. 用户角色

| 角色 | 能做什么 |
|---|---|
| 游客（未登录） | 浏览主集、公告、搜索、术语表、问题；不能 Fork / 提交 / 投票 / 评论 |
| 登录用户 | 以上 + Fork 主集、在编辑器里翻译、提交合并请求、对请求投票、评论、提问题 |
| 管理员（is_admin） | 以上 + 审核/合并/拒绝请求、一键合并全部、标记主集"已完成校对"、删除他人内容 |

---

## 3. 现有页面清单（路由 → 用途）

入口与全站：
- `/` — 首页（品牌展示页，目前偏视觉、与功能脱节）
- `/game` — 游戏列表（按平台分组的游戏卡片，每张卡有"公告/下载"和"协作翻译"两个入口）
- `/auth` — 登录 / 注册

单个游戏内（`/game/:platform/:gameSlug/...`，下面省略前缀）：
- `…` （basePath）— "社区"，公开译文集列表
- `/announce` — 公告 / 补丁下载页
- `/main` — **主集列表**（按类别分组：script/field/strtbl 等，可搜索，显示已完成计数）
- `/edit/:setId` — **编辑器**（左原文右译文逐句翻译；含术语表侧栏；可提交合并请求）
- `/requests` — **合并请求列表**（开放中 / 已关闭，带投票分数，含"下载改动""一键合并全部"）
- `/requests/:requestId` — 合并请求详情（diff 逐句对比、投票、评论、合并/拒绝）
- `/search` — 跨主集搜索原文/译文
- `/issues` — 问题反馈列表
- `/issues/:issueId` — 问题详情（评论、置顶）
- `/glossary` — 术语表（jp→zh 词条，管理员可增删改）

游戏内导航栏（GameNav）固定 7 项：公告 · 主集 · 社区 · 搜索 · 合并请求 · 问题 · 术语表

---

## 4. 核心流程（现状）

**新用户想参与翻译的路径：**
```
首页 → /game 选游戏 → 主集列表 → 点某个文件"Fork" → 跳到编辑器
→ 逐句翻译 → "提交合并请求" → 等管理员在 /requests 审核 → 合并进主集
```

**审核方（管理员）：**
```
/requests 看开放请求 → 点进详情看 diff → 投票/评论 → 合并 or 拒绝
（或在列表页"一键合并全部"）
```

---

## 5. 现有信息架构 / UX 的痛点（重点——希望重设计解决）

1. **首页与产品脱节**：`/` 是个偏艺术的品牌页（TopNav 写着 "ABOUT / WORK / CONTACT / SYSTEM OFFLINE"），完全没有引导用户进入翻译功能，新人不知道从哪开始。
2. **三个相似的列表容易混淆**：「主集」「社区（公开译文）」「合并请求」概念相近，新用户分不清该去哪。"社区"是别人公开的 Fork，"主集"是官方基准，关系没有可视化表达。
3. **Fork 心智负担重**：照搬 Git 的 Fork→PR 模型，但翻译用户多数不懂 Git。Fork 会产生大量副本，且 Fork 后才发现要去另一个页面提交、再去第三个页面等审核，链路太长、太分散。
4. **缺少"我的工作区"**：用户 Fork 的副本、提交的请求、提的问题散落各处，没有一个集中的"我的翻译 / 我的贡献"视图。
5. **进度不可见**：主集有"已完成校对"标记和计数，但整体翻译进度（多少句已翻 / 已校对 / 待翻）没有贯穿全站的进度可视化。游戏卡片上的 progress 是写死的 0%。
6. **导航是平铺的 7 项**，没有按"我要翻译 / 我要审核 / 我要查资料"分组，认知负担大。
7. **多游戏切换弱**：进入某游戏后，切换到另一个游戏要退回 `/game`，没有快速切换。

---

## 6. 设计语言（保持一致，沿用现有视觉）

深色科技感主题：
- 背景：`#07111e`（深蓝黑）；卡片面：`rgba(255,255,255,0.03~0.05)` + `1px` 浅边框
- 主文字：`#e0eaff`；次要文字：`rgba(200,220,255,0.4~0.7)`
- 主强调色（按钮/激活/链接）：`#5080ff`（蓝），hover `#6090ff`
- 状态色：成功/已合并绿 `#3fb950`、已完成 `#6ee7a0`、合并紫 `#a371f7`、拒绝/危险红 `#f85149`
- 圆角 8–12px；字体 Inter / system-ui
- 组件词汇：`btn-primary`（实心蓝）/`btn-ghost`（描边）；卡片网格；状态 dot 和 badge；左原文右译文的 diff 行

---

## 7. 希望设计 agent 产出什么

请基于以上，重新设计 **整站信息架构与关键页面流程**，目标：让一个不懂 Git 的普通玩家也能 3 步内开始翻译并看到自己的贡献。具体希望包含：

1. **新的站点地图 / 导航结构**：如何把现有 11 个页面重新组织、分组、命名，减少"主集/社区/合并请求"的混淆。
2. **重设计的首页 / 游戏首页**：一进来就清楚"这是什么、我能做什么、从哪开始"，带进度可视化。
3. **简化后的"翻译 → 提交 → 合并"流程**：是否能弱化 Fork 概念、把编辑器和提交合并并入一条更顺的路径。
4. **新增"我的工作区"**：集中展示我的译文、我的请求、我的问题、我的贡献统计。
5. **多游戏的切换与总览**入口。
6. 用上面第 6 节的深色设计语言，给出关键页面的高保真稿。

请先给出 **信息架构方案 + 站点地图**，再逐页出设计稿。
